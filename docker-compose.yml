version: '3.9'

services:
  app:
    build: .
    container_name: fatturahub
    restart: unless-stopped
    ports:
      - "5173:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/db/fatturahub.db
      - FILES_PATH=/app/data/files
