# Backend Assets

Questa cartella può contenere risorse statiche del backend, inclusi
fogli di stile XSLT per uso server-side futuro.

Attualmente la trasformazione XSLT avviene nel browser (client-side).
